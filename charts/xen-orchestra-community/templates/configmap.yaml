apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "xoc.fullname" . }}-config
data:
  config.toml: |
    [http]
    [[http.listen]]
    port = 80

    [redis]
    uri = "redis://{{ include "xoc.fullname" . }}-redis:{{ .Values.redis.port }}/0"
